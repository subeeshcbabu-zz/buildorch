'use strict';
var path = require('path'),
    assert = require('chai').assert,
    fs = require('fs');

describe('Sample Test', function () {

    

    before(function () {
        
        
    });

    after(function () {
        
    });

    // it('negative test case', function (next) {
    //     assert.ok(null);
    //     next();
    // });

    it('sample test', function (next) {

		next();
    });

});